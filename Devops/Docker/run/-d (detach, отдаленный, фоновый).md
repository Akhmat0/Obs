***Флаг для фонового запуска контейнера (работает, но не принимает входных данных с терминала)***

Пример:
```
docker run -d --name nginx -p 80:80 nginx
```

Чтобы проверить что он работает можем написать:
```
docker ps
```

